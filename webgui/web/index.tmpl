<!doctype html>
<html lang="ja">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>md2html Web GUI</title>
  <script src="https://unpkg.com/htmx.org@2.0.0"></script>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-900 font-sans p-8">
  <div class="max-w-3xl mx-auto bg-white p-6 rounded shadow">
    <h1 class="text-2xl font-bold mb-4">md2html – Web GUI</h1>

    <!-- アップロードフォーム（HTMXプレビュー） -->
    <form class="mb-6" hx-post="/preview" hx-target="#preview" hx-swap="innerHTML" enctype="multipart/form-data">
      <label class="block mb-2 font-semibold">Markdownファイルをアップロード：</label>
      <input type="file" name="mdfile" accept=".md" required class="mb-2">
      <button class="px-4 py-2 bg-blue-600 text-white rounded">プレビュー</button>
    </form>

    <!-- プレビュー結果（差し替え対象） -->
    <div id="preview" class="border border-gray-300 bg-gray-50 p-4 rounded">
      {{.HTML}}
    </div>

    <hr class="my-6">

    <!-- テキストエリアからHTMLファイルとしてダウンロード -->
    <h2 class="text-xl font-semibold mb-2">テキストから出力（HTMLダウンロード）</h2>

    <form method="post" action="/export">
      <textarea name="mdtext" placeholder="# Markdownを入力" class="w-full h-40 border p-2 mb-2"></textarea>
      <div>
        <button class="px-4 py-2 bg-green-600 text-white rounded">HTMLをダウンロード</button>
      </div>
    </form>
  </div>
</body>
</html>
